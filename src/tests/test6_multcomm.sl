func main(): void {
    let visible int = 1;

    /* Nvel 2: Comentrio dentro de comentário!
          Se a lgica estiver errada, o lexer vai parar 
          no prximo fechar-comenrio.
          let hidden int = 999; // Isso deve ser ignorado
    */

    print(visible);
}